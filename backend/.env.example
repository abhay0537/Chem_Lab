# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=MONGODB_URI=
# For MongoDB Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/chemlab

# JWT Configuration
JWT_SECRET=
JWT_EXPIRE=7d

# CORS Origin
CLIENT_URL=http://localhost:3000
